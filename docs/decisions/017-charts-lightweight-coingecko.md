| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state# ADR-017: Integración de Lightweight Charts + CoinGecko para HistoryChart y AllocationDonut
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Estado:** Aceptada
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Fecha:** 2026-05-21
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Contexto:** Los componentes `HistoryChart` y `AllocationDonut` mostraban datos hardcodeados (SVG estático con path fijo, donut con BTC 67%/ETH 19%/SOL 13%). Se necesitaba conectarlos a datos reales del portafolio del usuario.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state## Contexto
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading stateCaletaJS ya contaba con un flujo de datos establecido:
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- `HoldingsTable` agrega transacciones desde `localStorage` vía `aggregateHoldings()`.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- Tras fetchear precios de CoinGecko (`/coins/markets`), emite el evento `prices-updated` con los datos procesados.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- `StatsGrid` escucha `prices-updated` y actualiza sus tarjetas.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading stateLos charts no participaban en este flujo — renderizaban SVG/CSS estáticos sin conexión a datos reales.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state## Decisión
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading stateSe adoptó una **estrategia dual** para los dos componentes de charts:
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state### 1. HistoryChart → `lightweight-charts` (TradingView) + CoinGecko `/market_chart`
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Motor de renderizado:** Se reemplazó el SVG hardcodeado por la librería `lightweight-charts` v5.x (área series).
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Fuente de datos:** Nueva utilidad `getCoinHistory.js` que llama a `/coins/{id}/market_chart` de CoinGecko por cada coin única.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Capa de adaptación:** `chartDataAdapter.js` contiene `buildPortfolioHistorySeries(days, signal)` que:
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state  1. Agrega holdings crudos desde `localStorage` (deduplicando por `coinId`).
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state  2. Fetchela historia de precios de cada coin en paralelo.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state  3. Calcula `portfolioValue[día] = Σ(amount × price[día])` para cada día.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Lifecycle SPA:** El componente exporta `cleanupHistoryChart()` que se llama en `routes.js` antes de cada navegación, previniendo memory leaks.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **AbortController:** Las peticiones en vuelo se cancelan cuando el usuario cambia de período o navega fuera de la ruta.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Estados:** Loading spinner y empty state para portafolio vacío o errores de API.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state### 2. AllocationDonut → Evento `prices-updated` (datos ya procesados)
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Fuente de datos:** `buildAllocationData()` en `chartDataAdapter.js` lee de `localStorage` vía `getAggregatedHoldings()`.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Sin llamadas API adicionales:** No invoca CoinGecko directamente; usa `currentPrice` almacenado en el holding.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Renderizado:** SVG donut nativo con `stroke-dasharray`/`stroke-dashoffset` calculados dinámicamente según porcentajes reales.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Paleta:** 7 colores cíclicos del design system (`#0bd570`, `#3b82f6`, `#f43f5e`, `#f59e0b`, `#a855f7`, `#06b6d4`, `#10b981`).
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Empty state:** Mensaje inline cuando no hay holdings.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state### 3. Integración en Router
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- `initHistoryChart()` y `initAllocationDonut()` se llaman en `routes.js` tras el render del DOM.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- `cleanupHistoryChart()` se ejecuta al inicio de cada navegación para destruir la instancia previa del chart.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state## Consecuencias
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state### Positivas
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Datos reales:** Ambos charts muestran información actual del portafolio.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Sin duplicación de llamadas API:** `HistoryChart` hace sus propias llamadas a `/market_chart`; `AllocationDonut`Lee datos existentes.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Performance:** `lightweight-charts` es ~45KB gzipped, compatible con Webpack 5.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **UX:** Selector de período funcional (1d, 7d, 30d, 90d, All) con actualización en tiempo real.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Memory safety:** Cleanup explícito en SPA evita listeners y chart instances huérfanos.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state### Negativas
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Rate limit CoinGecko:** Plan Demo = 30 calls/min. Cada cambio de período = N llamadas (N = coins únicas). Aceptado como deuda técnica (ver ADR-018).
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **Dependencia nueva:** `lightweight-charts` es la primera dependencia de UI significativa (excluyendo Tailwind/PostCSS).
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state- **`AllocationDonut` no es reactivo al evento `prices-updated`:** Lee `localStorage` directamente en su `initAllocationDonut()`. Un refresh manual (botón en HoldingsTable) no actualiza el donut automáticamente hasta la próxima navegación.
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state## Alternativas Consideradas
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| Alternativa | Razón de descarte |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state|---|---|
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| **Chart.js** | Bundle más pesado (~200KB), diseño genérico, sin soporte nativo para series financieras. |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| **D3.js** | Demasiado low-level para una serie de área simple; overhead innecesario. |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| **Canvas API nativo** | Requiere implementar ejes, crosshair, tooltips, escalas logarítmicas desde cero. |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| **SVG nativo para HistoryChart** | Performance degradada con >100 puntos de datos; sin zoom/pan nativo. |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| **AllocationDonut con `prices-updated`** | Se evaluó y se eligió leer localStorage directamente para mantener la misma fuente de datos que `getAggregatedHoldings()` en `chartDataAdapter.js`. |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state## Archivos Afectados
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| Archivo | Acción | Descripción |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state|---|---|---|
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| `package.json` | Modificado | Se añadió `lightweight-charts@^5.2.0` como dependencia |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| `src/utils/getCoinHistory.js` | Nuevo | API helper para `/coins/{id}/market_chart` |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| `src/utils/chartDataAdapter.js` | Nuevo | Lógica de negocio: `buildPortfolioHistorySeries()`, `buildAllocationData()`, `getAggregatedHoldings()` |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts con selector de período |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| `src/components/AllocationDonut.js` | Refactorizado | Datos hardcodeados → SVG dinámico con `buildAllocationData()` |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| `src/router/routes.js` | Modificado | Importación y llamada a `initHistoryChart`, `initAllocationDonut`, `cleanupHistoryChart` |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state| `src/pages/About.js` | Modificado | Atribución a TradingView (ya existía en la sección Tecnologías) |
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state---
| `src/components/HistoryChart.js` | Refactorizado | SVG → lightweight-charts v5 con selector de período, HMR handlers y cleanup de loading state*Última actualización: 2026-05-23*
